<!-- 本文件由 machine/tools/render_human.py 从机器平面生成。请勿手写——下次渲染会覆盖。 -->
<!-- 事实源：machine/facts/features.json、data_contract.yaml、config.yaml -->
<!-- 上限 200 行 -->
<!-- 纯净门：本文件「一、功能清单」章节出现 phase/gate/review/replay/audit 等日志词 -> 渲染 FAIL -->

# 系统架构

一句话：这个系统有哪些功能、数据怎么从头走到尾、关键参数为什么这么定。

## 一、功能清单

只列**功能本身**。做了什么、过了哪些关，去看 `05_执行与验收.md`。

| 编号 | 功能 | 状态 | 证据 |
|---|---|---|---|
| `FEAT-HUB-001` | `Vite` + `TypeScript` 前端工程 | 进行中 | 已声明 |
| `FEAT-HUB-002` | 三维主场景、程序化模型、粒子场与可选泛光后处理（`Three.js`） | 进行中 | 已声明 |
| `FEAT-HUB-003` | 物理世界与重力实验区刚体（`Rapier`） | 进行中 | 已声明 |
| `FEAT-HUB-004` | 滚动节奏与显影（`Lenis` + `GSAP`） | 进行中 | 已声明 |
| `FEAT-HUB-005` | 档案、星云、夜航、花园四个独立模式 | 进行中 | 已声明 |
| `FEAT-HUB-006` | 六个主模型切换（星图仪、漂浮岛、档案书、宇宙罗盘、黑金花园、能量核心） | 进行中 | 已声明 |
| `FEAT-HUB-007` | 滚动重力（方向、速度、横向滚轮、指针与空格键共同影响） | 进行中 | 已声明 |
| `FEAT-HUB-008` | 五卡整卡入口星图 | 进行中 | 已声明 |
| `FEAT-HUB-009` | 项目卡上线状态徽章（已上线 / 可部署 / 受保护） | 进行中 | 已声明 |
| `FEAT-HUB-010` | 画质分档（`low` / `medium` / `ultra`） | 进行中 | 已声明 |
| `FEAT-HUB-011` | 降低动效的无障碍降级 | 进行中 | 已声明 |
| `FEAT-HUB-012` | `Cloudflare` 边缘静态资源部署 | 进行中 | 已声明 |
| `FEAT-HUB-013` | 单页回退（未知路径回 `index.html`） | 进行中 | 已声明 |

**证据一栏怎么看：** 「已提取」= 从代码或配置里核对过；「已声明」= 目前只有文档说法，还没对过代码。

## 二、数据从哪到哪

只消费静态项目事实：`projects.json` 定义五张上线卡的状态型元数据（不含任何私密数据）；`index.html` 与场景模块渲染；构建前校验脚本扫描文本。公共静态面不连接任何本机运行面或私有原始数据。

## 三、关键参数为什么这么定

当前值和改哪里在 `06_运维手册.md`，这里只讲**为什么是这个值**。

| 参数 | 为什么定这个值 |
|---|---|
| 画质档-低 | 降低动效、移动端、低性能或显式指定低画质时启用，可用性优先。 |
| 画质档-中 | 默认桌面。 |
| 画质档-高 | 高性能桌面。 |
| 重力公式 | 把滚动当作输入力而非装饰。 |
| 兼容级别 | 五个网关节点均为静态优先的公开适配层，不接私密核心。 |
| 上线状态 | 已上线必须有当前访问证据；空跑只能标可部署；受访问控制标受保护。 |

## 四、数据长什么样

| 数据 | 关键字段 | 主键 |
|---|---|---|
| 项目卡 | `id`, `name`, `category`, `compatibilityLevel`, `deploymentStatus`, `liveUrl`, `fallbackUrl`, `mode` | `id` |
