# Models

Put future `.glb` or `.gltf` assets here.

The first release uses procedural Three.js models so the site can build and deploy without binary assets. Keep future model filenames stable and wire them through `src/scene/models/`.
