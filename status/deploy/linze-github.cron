# 装到 /etc/cron.d/linze-github —— GitHub Engineering Plane 采集(三层)。
#   fast    每 1 分钟:单条 GraphQL,cost 约 2 点、约 3 秒 -> 每小时约 120/5000 点(2.4%),安全。
#   traffic 每 15 分钟:只打 traffic 两个端点(REST 18 请求)+ 逐日提交 GraphQL(cost 1)。
#   deep    每小时 07 分:REST 深采(release / 子项目 / Actions 日频 / 账单),约 3-4 分钟。
#
# ★ 为什么要单独一层 traffic:GitHub 的浏览/克隆**一天只发布一次、且恒定滞后约 2 天**
#   (实测 07-27 当天窗口只到 07-25)。这是上游限制,采得再密也不会让它提前发布。
#   单独一层的意义只有一个:新数据一发布,15 分钟内就能被抓到并在页面上高亮"刚到达",
#   而不是最坏等满一个小时。想看真正实时的访问,看的是本站自己的 access log(collect.py)。
# flock -n:上一轮没跑完就跳过本轮,避免重入堆积。
SHELL=/bin/bash
PATH=/usr/local/sbin:/usr/local/bin:/usr/sbin:/usr/bin:/sbin:/bin
*/1 * * * * ubuntu flock -n /tmp/linze-gh-fast.lock python3 /srv/linze/apps/status/collector/collect_github.py fast >> /srv/linze/apps/status/github.log 2>&1
3,18,33,48 * * * * ubuntu flock -n /tmp/linze-gh-traffic.lock python3 /srv/linze/apps/status/collector/collect_github.py traffic >> /srv/linze/apps/status/github.log 2>&1
7 * * * * ubuntu flock -n /tmp/linze-gh-deep.lock python3 /srv/linze/apps/status/collector/collect_github.py deep >> /srv/linze/apps/status/github.log 2>&1
